{
  "name": "tiktok-slack-companion",
  "version": "0.0.0",
  "module": "src/server.js",
  "devDependencies": {
    "prettier": "^3.0.3"
  },
  "license": "gpl-3.0-only",
  "scripts": {
    "test": "jest",
    "tdd": "jest --watch",
    "start": "node src/main.mjs",
    "dev": "node --env-file=.env src/main.mjs",
    "tunnel": "npx localtunnel --port 3000 --subdomain tiktok-companion"
  },
  "dependencies": {
    "@slack/bolt": "^3.14.0",
    "@slack/web-api": "^6.9.0",
    "axios": "^1.5.0",
    "jest": "^29.7.0"
  }
}
